
#include <stdio.h>
#include <stdlib.h>
int main(void) {
	int x=0 , y=0, sum;
	printf("Enter two numbers :");
	fflush(stdin);fflush(stdout);
	scanf("%d %d",&x,&y);
	sum=x+y ;
	printf("summation is :%d",sum);

}
